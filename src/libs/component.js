export class Component {
    constructor(element) {
        this.element = element;
    }

    /**
     * Function which call when component initialized
     * @interface
     */
    initialize() {}
}
